// map, filter , reduce

// map
// map method is used to create a new array from the existing one by applying the function to each element of the array

const nums = [1, 2, 3, 4];

const multiplythrere = nums.map((num, i) => {
  return num * 3 + 1;
});
console.log(multiplythrere);

// //filter
// filter takes each element in a array and it applies a condititonal statement against it
// if the
